widthOfMainLogo - получение ширины логотипа на главной странице сайта

isExistingMainManu - булевое значение в зависимости от нахождения в DOM меню с навигацией

textOfCaption - возвращает текст одного из заголовков главной страницы (в нашем случае "Видео дня")

selectSports - возвращает DOM - элемент, содержащий список видов спорта для выбора пользователем

footballSportIsExisting - возвращает булевое значение в зависимости от нахождения в вышеуказанном списке (selectSports) вида "футбол"

isExistingMainHeader - возвращает булевое значение в зависимости от нахождения в DOM шапки сайта

toVisitFantasyPage - переход на вкладку "Fantasy" в навигации сайта

toUseVkLink - переход по ссылке на группу Вконтакте в подвале сайта

toUseAssembleTeamLink - переход по ссылке "Собрать команду" на странице "Fantasy"

getTextOfCaptionNewLink - возвращает текст заголовка страницы после нажатия на ссылку "Собрать команду"

backToMainPage - возвращает на главную страницу Sports.ru со страницы после нажатия "Собрать команду"

goToMainPage - возвращает на главную страницу сайта

loginMyProfile - осуществляет вход в профиль с тестовыми данными

getDateOfRegistration - осуществляет переход в личный кабинет и возвращает дату регистрации пользователя
